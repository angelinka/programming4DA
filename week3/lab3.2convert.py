# program that takes in a float amount of dollars,
#  and returns absolute amount in cent

fl = abs(float(input('Enter float amount in dollars:')))
cent = int(fl * 100)
print ('That amount in cent is:', cent)